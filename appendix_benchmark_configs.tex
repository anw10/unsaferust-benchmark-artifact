\section{Benchmarks Configurations}
\label{sec:appendix_benchmark_configs}

We provide the run configurations for our selected benchmarks.

\begin{table*}[h!]
\centering
\caption{Benchmark configurations for our 18 selected crates. Default: cargo bench was used and benchmark suite was provided by developers.}
\label{table:benchmark_configs}
{\scriptsize


\begin{tabular}{@{}l l@{}}
    \toprule
    {\bf Crate} & {\bf Bench Run Config} \\
    \midrule
    {matrixmultiply} & Default \\
    {arrayvec} & Default \\
    {ndarray} & Default \\
    {hashbrown} & Default \\
    {async-task} & Default \\
    {getrandom} & Default \\
    {httparse} & Default \\
    % {openssl} & Default \\
    {smallvec} & Default \\
    {memchr} & Using \emph{rebar}, a rust tool that runs cargo under the hood  \\
    {jpeg-decoder} & Default \\
    {semver} & Default \\
    {rayon} & Build rayon-demo folder with \emph{cargo build --release}, only call \emph{nbody bench} with \emph{--bodies 500} as parameters  \\
    {jni} & Using cargo bench but with \emph{--features invocation} passed as arguments \\
    {parkinglot} & Build \emph{benchmarks} folder using \emph{cargo build --release}. Parameters for executables: \emph{./mutex 2 4 10 2 4} and \emph{./rwlock 4 4 4 10 2 4} \\
    {simd-json} & Default \\
    {ring} & Build and run all benchmarks by passing \emph{--benches} to default \emph{cargo bench} command  \\
    {tokio} & Default \\
    {petgraph} & Default \\
    \bottomrule
\end{tabular}
}
\end{table*}
